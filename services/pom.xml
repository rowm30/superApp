<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- Parent ka bhi ek parent hai: Spring Boot.
         Isse plugin versions, java settings, sab neeche tak beh jaate hain. -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>com.superapp</groupId>
    <artifactId>services</artifactId>
    <version>0.0.1-SNAPSHOT</version>

    <!-- "pom" = ye khud koi jar nahi banata, sirf bachchon ko jodta hai -->
    <packaging>pom</packaging>
    <name>superapp-services</name>

    <!-- Bachche. Ye FOLDER ke naam hain. Naya service = yahan ek line. -->
    <modules>
        <module>profile-service</module>
        <module>edge-gateway</module>
        <module>product-service</module>
        <module>order-service</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2025.1.2</spring-cloud.version>
    </properties>

    <!-- Spring Cloud ki version list. Sirf yahan, ek jagah.
         Bachche version likhe bina dependency maang sakte hain. -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>